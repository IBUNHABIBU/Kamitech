<div class="form form--mid">
  <div class="form__container">
<%= form_with(model: partner) do |form| %>
  <% if partner.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(partner.errors.count, "error") %> prohibited this ourservice from being saved:</h2>

      <ul>
        <% partner.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-box">
      <%= form.text_field :Title, required: "required" %>
      <span>Title</span>
      <i></i>
  </div>
  <div class="input-box">
      <%= form.text_field :Description, required: "required" %>
      <span>Description</span>
      <i></i>
  </div>
  <div class="input-box">
  <%= form.file_field :image, required: "required" %>
  </div>
<%= form.submit "Create", class: "btn btn--mid" %>

<% end %>
  </div>
</div>
