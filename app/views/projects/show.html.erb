
<%= render @project %>

<div>
  <%= link_to "Back to projects", projects_path %>
  <% if current_user %>
    <%= button_to "Destroy this project", @project, method: :delete, class: "btn" %>
    <%= link_to "Edit this project", edit_project_path(@project) %> |
  <% end %>
</div>
